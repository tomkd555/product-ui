{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'if command -v python >/dev/null 2>&1; then exec python \"$0\"; else exec python3 \"$0\"; fi' \"${CLAUDE_PLUGIN_ROOT}/hooks/ui_text_lint.py\""
          }
        ]
      }
    ]
  }
}
